// Supabase client helper removed.
// This project uses Drizzle + Postgres direct connections for migrations and runtime.
// The file was intentionally left as a stub to avoid introducing @supabase/supabase-js
// as a dependency when Supabase is used only as a Postgres host (Option A).

export {};
